#include <stdio.h>

int main(void)
{
	float t, v;
	printf("write the Volume:");
	scanf_s("%f",&B);
	printf("write the time:");
	scanf_s("%d", &t);
	v = (B / t);
	printf("speed is:%d", v);
	system("pause");
	return 0;
}
